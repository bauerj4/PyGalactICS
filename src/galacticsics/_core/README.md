# Compiled extensions (future)

Direct f2py bindings of individual Fortran routines may live here in a future
release. **Today**, legacy numerics are isolated under ``legacy/fortran/`` and
invoked via ``galacticsics.legacy.runner``.

See ``legacy/README.md`` and ``galacticsics.potential.solver.solve_potential``.
